# Development

See the README for install/build instructions.

- `src/` contains the plugin source.
- `lib/` contains built artifacts used by npm/GitHub installs.
- Run `npm run typecheck` and `npm run build` before release.
